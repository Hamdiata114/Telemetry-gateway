This project implements a UDP-based telemetry ingress gateway designed to safely accept untrusted input and forward only validated, bounded data to downstream systems.
The focus of this project is systems-security reasoning: trust boundaries, invariants, failure modes, and explicit policy enforcement.
Best-effort delivery is intentional; drops are acceptable and observable.cd ..
